# Uncomment the next line to define a global platform for your project
source 'https://github.com/CocoaPods/Specs.git'
# platform :ios, '10.0'

target 'EthWatch' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  pod 'Alamofire', '~> 4.0'

  # Pods for EthWatch

  target 'EthWatchTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'EthWatchUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'EthWatch WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EthWatch WatchKit App
  pod 'Alamofire', '~> 4.0'
end

target 'EthWatch WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for EthWatch WatchKit Extension
  pod 'Alamofire', '~> 4.0'
end
